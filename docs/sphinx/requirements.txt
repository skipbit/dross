# Pinned: a required check installs these, so a release on its own must not
# be able to fail a pull request. Dependabot proposes the updates, and the
# documentation build in check.yml decides whether they hold.

# Sphinx and theme
sphinx==9.0.4
furo==2025.12.19

# Sphinx extensions
breathe==4.36.0
sphinx-copybutton==0.5.2
sphinx-tabs==3.5.0
sphinx-design==0.7.0
sphinx-autobuild==2025.8.25

# Optional for development
sphinx-lint==1.0.2
